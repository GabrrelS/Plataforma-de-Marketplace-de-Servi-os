name: Microservices

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

jobs:

  discover:

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v4

      - name: Listar microsserviços
        run: |
          find . -type f -name "*.csproj"